# MCP Servers Environment Configuration

# Obsidian MCP Server
OBSIDIAN_VAULT_PATH=/vault

# PostgreSQL MCP Server
# Production PostgreSQL connection for MCP server (restricted read-only access)
POSTGRES_DATABASE_URI=postgresql://mcp_server:mcp_secure_pass_2024!@host.docker.internal:5432/wix_main
